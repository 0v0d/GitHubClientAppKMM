import SwiftUI
import Shared

struct ContentView: View {
    var body: some View {
        NavigationView {
            InputScreenView()
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
